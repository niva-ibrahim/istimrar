import React from "react";
import ReactDOM from "react-dom/client";
import AmbitionMap from "./AmbitionMap.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AmbitionMap />
  </React.StrictMode>
);
